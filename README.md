puppet-firewall_wrapper
=======================

This module is made to wrap around the firewall module since foreman does not manage defines.

ports should be in the forman tcp_port_src: prefix => 012
